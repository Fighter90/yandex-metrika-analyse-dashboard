# Руководство пользователя

> 🇷🇺 Русский · [🇬🇧 English](en/user-guide.md)

Как пользоваться дашбордом и генерировать отчёты. Для команды трека «Конверсии и лидген».

> ✅ **Статус: рабочий продукт (v2.7.0).** Дашборд из 9 страниц: Обзор, Трафик, Поведение, Воронка,
> Цели, Отчёт, История, Настройки, Справка. Глобальные фильтры до 1 года, AI-анализ отчёта
> (включая AI-гипотезы и AI Decision Log), GOST-форматированные DOCX/PDF. Запуск: `./setup.sh`.

## 1. Что это и зачем

Инструмент отвечает на один вопрос: **откуда берутся платные билеты и как добрать до 300**.
Он не заменяет Яндекс.Метрику, а превращает её сырые цели в понятную картину:
разбивку по каналам, воронку «визит → заявка → оплата», и дисциплину гипотез.

Главный принцип: **заявка ≠ оплата**. Цель в Метрике (заявка) и реальная оплата
разделены везде в интерфейсе.

## 2. Запуск

См. [runbook.md](runbook.md). Коротко: `./setup.sh` (или `pnpm install && ./init.sh && ./run.sh`) →
браузер на `http://localhost:5173`.

## 3. Дашборд: страницы

В дашборде **9 страниц** (верхнее меню; на экранах < 1024px — гамбургер-меню).

Глобальные фильтры (шапка, на всех страницах кроме «Настройки» и «Справка»): пресеты периода
**7д / 14д / 30д / 90д / 1г**, кастомный date picker (от/до, макс. 365 дней), переключатель сегмента
**B2C / B2C+B2B / B2B**, чекбокс «Архивные цели».

| Страница      | URL         | Что показывает                                                                                                                                                                                                                                   |
| ------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Обзор**     | `/`         | KPI-стрип (цель 300; метка **«Оплат»** для целей-покупок); **дайджест за неделю** (визиты + заявки с WoW-дельтой, топ-канал, главное слабое место); графики визитов/заявок по дням; микс каналов; weak spots; **подписи под графиками** 🟢/🔴/💡 |
| **Трафик**    | `/traffic`  | бар каналов, визиты vs заявки, таблица каналов с CR, **UTM-Sankey** («Поток: источник → кампания → заявки»), insights, подписи под графиками                                                                                                     |
| **Поведение** | `/behavior` | страницы входа (startURL) и выхода (exitURL): графики + таблицы (визиты / отказы / заявки / CR), рекомендации, подписи под графиками                                                                                                             |
| **Воронка**   | `/funnel`   | воронка «заявка ≠ оплата»: Визиты → Заявки B2C → B2B pipeline → Оплачено B2B; **воронка по каналам**; **CR по каналам**; анализ потерь; B2B по этапам; подписи под графиками                                                                     |
| **Цели**      | `/goals`    | прогресс-ринг к 300 билетам, метрики, B2B-сделки, рекомендации на основе данных                                                                                                                                                                  |
| **Отчёт**     | `/report`   | сборка snapshot; **AI-гипотезы** (проблемные и решенческие, секции «Гипотезы роста (AI)») и **AI Decision Log** внутри отчёта; AI-анализ (5 секций, HTML); отчёт прямо на экране; экспорт DOCX/PDF; перестроение                                 |
| **История**   | `/history`  | список сохранённых снапшотов (горизонтальный скролл); «Просмотреть» открывает снапшот без перегенерации                                                                                                                                          |
| **Настройки** | `/settings` | OAuth-токен, Client ID/Secret, COUNTER_ID, GOAL_ID (select из Метрики), ANTHROPIC_API_KEY; «🔄 Обновить данные» с прогресс-баром; сворачиваемая секция **«B2B-пайплайн»** для ввода B2B-сделок                                                   |
| **Справка**   | `/help`     | встроенная документация: описание всех страниц, фильтров, FAQ, глоссарий                                                                                                                                                                         |

> Страницы `/hypotheses` и `/decisions` перенаправляют на `/report`; `/b2b` — на `/settings`. Страницы «Настройки» и «Справка» — без панели фильтров.

> Страницы «Настройки» и «Справка» — без панели фильтров.

### Подписи под графиками (v2.6.0)

Под каждым графиком и таблицей дашборда отображается блок с тремя строками:

- **🟢 Корректно** — источник данных в SQLite и что он означает.
- **🔴 Внимание** — известное ограничение (например, exitURL не отдаётся Метрикой).
- **💡 Рекомендация** — что стоит проверить или как использовать данные.

Это расширяет принцип anti-hallucination: пользователь видит не только цифру, но и её происхождение.

### Дайджест за неделю (страница «Обзор»)

Карточка в верхней части страницы «Обзор» показывает за последние 7 дней:
визиты и заявки с WoW-дельтой (сравнение с предыдущими 7 днями), топ-канал по визитам,
главное слабое место. Данные берутся из `channel_stats`.

### Ограничение: страницы выхода

Яндекс.Метрика не отдаёт `ym:s:exitURL` для счётчика ProductCamp, поэтому таблица
`exit_page_stats` остаётся пустой. Блок «Страницы выхода» автоматически скрывается при
отсутствии данных — это не баг, а известное ограничение API (см. подпись 🔴 под блоком).

### Откуда эта цифра?

Любое число в дашборде и отчёте прослеживается до сырого ответа Метрики в `raw_responses` (SQLite).
Никаких выдуманных цифр — это инвариант anti-hallucination.

## 4. Гипотезы и Decision Log (AI внутри отчёта)

Начиная с v2.7.0 отдельных страниц «Гипотезы» и «Решения» в навигации нет. Вместо этого:

- **AI-гипотезы** (проблемные и решенческие) генерируются при сборке снапшота и сохраняются
  в `snapshot.generatedHypotheses`. В отчёте они отображаются в секциях
  «Решенческие гипотезы (AI)» / «Проблемные гипотезы (AI)» в структурированном формате
  методологии (ICE, допущения, методы проверки, светофор).
- **AI Decision Log** (предполагаемые решения) генерируется аналогично и сохраняется в
  `snapshot.generatedDecisions`. В отчёте — секция «Decision Log (предполагаемые решения)».

Оба блока доступны в **отчёте на экране** и экспортируются в DOCX/PDF.
Прямые URL `/hypotheses` и `/decisions` перенаправляют на `/report`.

## 5. B2B-пайплайн (в Настройках)

Ручной ввод B2B-сделок перенесён в сворачиваемую секцию **«B2B-пайплайн»** на странице
**Настройки** (`/settings`). Этапы: lead → negotiation → invoiced → paid. Оплаченные билеты
(paid) по-прежнему засчитываются в KPI 300 и учитываются на страницах «Воронка» и «Цели».
Прямой URL `/b2b` перенаправляет на `/settings`.

## 6. Генерация отчётов (включая AI-гипотезы и AI DL)

На странице **Report**:

1. Выберите период в шапке и нажмите **«Сформировать snapshot»** — соберётся и сохранится
   неизменяемый `ReportSnapshot`, покажется сводка KPI и **полный отчёт прямо на экране**
   (тот же контент, что в DOCX/PDF).
2. (Опц.) **«Сгенерировать AI-анализ»** — вызывает Anthropic из чисел снапшота и добавляет
   помеченную секцию «AI-анализ». Нужен `ANTHROPIC_API_KEY`; без него — понятное сообщение, отчёт
   соберётся без этой секции.
3. **Export DOCX** или **Export PDF** — рендерит файл в `data/reports/{snapshotId}.{docx|pdf}`.

Отчёт **детальный**, в GOST-форматировании (Times New Roman 14pt, титульная страница, оглавление,
нумерованные разделы, номера страниц; поддерживается markdown — таблицы, жирный/курсив, списки).
Секции: Cover, Executive Summary («заявка ≠ оплата»), Методология, Воронка визит→заявка→оплата,
Анализ по каналам, **Решенческие/Проблемные гипотезы (AI)** (из `snapshot.generatedHypotheses`),
**Decision Log (предполагаемые решения)** (из `snapshot.generatedDecisions`), AI-анализ (если
сгенерирован), топ-разбивки (UTM, гео+устройство, страницы входа/выхода), Data Appendix.

Отчёт строится из неизменяемого **snapshot**: один и тот же `snapshotId` даёт один и тот же контент в
DOCX, PDF и на экране (в render-пути нет `Date.now()`/LLM — AI-нарратив генерится один раз и
сохраняется; общий источник контента — `reportSections` из `@pca/shared`). PDF требует локального
Chrome через `PUPPETEER_EXECUTABLE_PATH`; DOCX — без зависимостей.

## 7. Принципы, о которых стоит помнить

- Любая цифра прослеживается до `raw_responses` — если не прослеживается, это баг.
- B2B входит в KPI 300 и ведётся вручную через секцию «B2B-пайплайн» в Настройках (таблица `b2b_manual`, CRUD через `POST /api/b2b`).
- Методология из `.claude/skills/` менять без отметки в
  `docs/methodology-hypotheses.md` нельзя.
