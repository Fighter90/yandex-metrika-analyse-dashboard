# NNN — <Краткое название фичи>

> Статус: `draft` · Автор: <имя> · Дата: YYYY-MM-DD · PR: —

## 1. Проблема и контекст

Что не так сейчас и почему это важно для команды трека «Конверсии и лидген». Ссылки на данные,
существующий код, методологию (Воронкова/ICE/DD), предыдущие спеки/ADR.

## 2. Цель

Одним абзацем: что должно стать возможным после реализации. Чего фича **не** делает (вне scope).

## 3. Критерии приёмки (измеримые)

- [ ] Конкретное проверяемое утверждение (станет тестом).
- [ ] …
- [ ] Инварианты проекта соблюдены: «заявка ≠ оплата», прослеживаемость до `raw_responses`, в
      render-пути отчёта нет `Date.now()`/LLM, 100% покрытие.

## 4. Влияние

- **Данные / БД:** новые таблицы, миграции, ключи (история по дням сохраняется).
- **API:** новые/изменённые эндпоинты, схемы, коды ответов.
- **Shared (`@pca/shared`):** типы, валидаторы, конфиг.
- **UI:** страницы, состояния (loading/error/empty/success), фильтры.
- **Конфиг / env:** новые переменные, дефолты, обратная совместимость.
- **Документация:** какие файлы обновить (README RU+EN, docs, QA).

## 5. План реализации (TDD-шаги)

1. <тест → код> …
2. …

Список затрагиваемых файлов с краткой ролью каждого.

## 6. Альтернативы и решение

Рассмотренные варианты и почему выбран этот (фиксируется как мини-ADR; при необходимости — отдельная
запись в `docs/decisions/`).

## 7. Открытые вопросы

- … (спека не принимается, пока остаются блокирующие вопросы)
